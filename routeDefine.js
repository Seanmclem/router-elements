class RouteDefine extends HTMLElement {
    // constructor(){
    //     super();
    // };
    //basically doesn't need any supporting code at this point.
    //only exists for structured attibutes on <router>'s children
}
customElements.define('route-define', RouteDefine);